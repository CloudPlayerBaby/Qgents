# Qgents 性能优化计划（快速见效版）

## 1. 目标与边界

目标是在不改变 Task -> TaskStep -> TaskRun 业务流程、不新增公开接口、不改数据库表的前提下，尽快回答三个问题：

1. 慢在哪里：编排、LLM、Worker、GitHub、测试还是 SSE。
2. 堵在哪里：哪个线程池排队、拒绝或长期占满。
3. 失败在哪里：失败码、重试次数和外部调用耗时是否相关。

本计划优先做低风险、可回滚的配置和观测改动。暂不直接重写 SSE、引入消息队列或做大规模异步架构调整。

## 当前执行进度

- [x] 接入 Actuator 与 Prometheus 指标依赖，并暴露受控监控端点。
- [x] 三个异步执行器参数配置化，保留原有默认容量并增加非法配置快速失败。
- [x] 编排/测试/超时执行器增加 active、pool size、queue size 指标。
- [x] Worker HTTP 请求增加耗时、成功/失败次数指标。
- [x] SSE 增加活跃连接数、轮询耗时和发送失败指标，并保证连接关闭只计数一次。
- [x] 在 WSL 用户目录安装 Prometheus 3.14.0，并提供本地抓取配置 `monitoring/prometheus/prometheus.yml`。
- [ ] 根据线上指标决定是否进行共享 SSE 调度器或数据库索引优化。

## 2. 当前高风险点

- `EventService` 的 SSE 连接当前使用固定大小为 8 的线程池，每个连接占用一个轮询线程；连接数超过 8 后会排队。
- `taskOrchestratorExecutor` 使用 core=2、max=4、queue=100，同时承载任务编排、恢复和部分 MR 自动化。
- `testExecutionExecutor` 使用 core=2、max=4、queue=100，同时承载普通 TestRun、Dry Run 和恢复任务。
- Worker、LLM、GitHub 都是外部调用，若没有统一耗时和超时记录，只能看到最终失败，无法区分网络慢、服务慢和线程池等待。
- `EventService.publish()` 需要项目锁、事件落库、成员查询和实时广播，事务链路较长，应先观测，再决定是否拆分。

## 3. 第一阶段：当天完成的观测与配置化（P0）

### 3.1 接入 Actuator + Micrometer

**修改位置**

- `pom.xml`
- `src/main/resources/application.yaml` 及各环境配置

**内容**

- 增加 Spring Boot Actuator 和 Micrometer Prometheus 依赖。
- 暴露 `health`、`info`、`metrics`、`prometheus`；生产环境通过配置控制访问权限，不开放敏感端点。
- 为端点增加认证或仅绑定内网，避免把运行指标暴露给普通用户。

**验收**

- 应用能启动；`/actuator/health` 返回 UP。
- Prometheus 端点能看到 JVM、HTTP 请求和线程池基础指标。
- 不改变现有业务 API 和鉴权行为。

### 3.2 将执行器参数配置化

**修改位置**

- `TaskOrchestrationAsyncConfiguration.java`
- `TestExecutionAsyncConfiguration.java`
- 新增或复用 `@ConfigurationProperties` 配置类
- `application.yaml`

**建议初始配置（可按机器资源调整）**

```yaml
qgents:
  executor:
    orchestration:
      core-size: 2
      max-size: 4
      queue-capacity: 100
    test-execution:
      core-size: 2
      max-size: 4
      queue-capacity: 100
    task-run-timeout:
      core-size: 2
      max-size: 4
      queue-capacity: 64
```

**规则**

- 保留有界队列，禁止改成无界队列。
- 为拒绝策略、关闭等待时间和线程名前缀保留配置项。
- 不在第一阶段盲目扩大线程数；先用指标确认队列确实是瓶颈。

**验收**

- 启动日志打印最终生效的非敏感参数。
- 配置错误时应用快速失败并给出明确字段名。
- 单元测试覆盖默认值和边界值。

### 3.3 增加统一结构化性能日志

**建议统一字段**

```text
requestId
traceId（如已有链路追踪则沿用）
taskId
taskRunId
taskStepId
workspaceId
sandboxId
operation
phase
attempt
durationMs
outcome
errorCode
```

**重点埋点位置**

- `TaskOrchestrator`：编排总耗时、阶段切换、排队等待、最终结果。
- `AgentDispatcher` / LLM 客户端：Agent 角色、调用耗时、轮次、Tool Calls 次数、超时和错误码。
- `SandboxWorkerClient`：创建 Sandbox、读写文件、执行测试、生成 Diff、commit、push 等操作耗时。
- GitHub 客户端：请求耗时、HTTP 状态、重试次数、稳定错误码（不得记录 Token）。
- `TestRunExecutionService`：快照准备、测试启动、测试完成、测试失败原因。
- `EventService`：SSE 连接建立/关闭、轮询耗时、每批事件数量、发送失败。

**安全要求**

- ID 放结构化日志字段，不作为 Prometheus 标签。
- 不记录 Token、Cookie、完整请求体、私钥、环境变量、原始命令输出或完整模型上下文。
- `operation`、`phase`、`outcome`、`errorCode` 使用有限枚举，避免日志字段无限膨胀。

## 4. 第二阶段：一到两天完成的指标与保护（P1）

### 4.1 统一计时器与错误指标

使用 Micrometer `Timer` 和 `Counter` 增加以下指标：

```text
qgents.task.orchestration.duration
qgents.task.orchestration.total
qgents.agent.run.duration
qgents.agent.run.total
qgents.worker.request.duration
qgents.worker.request.total
qgents.github.request.duration
qgents.github.request.total
qgents.test.run.duration
qgents.test.run.total
qgents.sse.poll.duration
qgents.sse.send.failures
```

建议标签仅使用低基数值：

```text
phase=PLAN/CODING/TESTING/REVIEWING/DELIVERY
role=PLANNER/DEVELOPER/TESTER/REVIEWER
operation=createSandbox/executeTool/push/createMr/runTest
outcome=SUCCEEDED/FAILED/TIMEOUT
errorCode=稳定错误码
```

禁止把 `taskId`、用户 ID、仓库名、分支名作为指标标签。

### 4.2 线程池运行指标

为三个执行器记录：

- active：当前活动线程数；
- pool size：当前线程数；
- queue size：等待任务数；
- completed：完成任务数；
- rejected：拒绝任务数；
- task wait duration：进入执行器到真正开始执行的等待时间。

当队列达到阈值时，业务应返回已有的“已排队/稍后重试”语义或稳定错误码，不能让 HTTP 请求无限等待。

### 4.3 外部调用超时和重试观测

- 为 Worker、LLM、GitHub 的每次调用记录 `attempt`、`durationMs`、`timeout` 和 `errorCode`。
- 保留现有重试语义，不在本阶段无条件增加重试次数。
- 只对明确的瞬时错误重试；参数错误、权限错误、基线不存在、补丁无法应用等确定性错误不重试。
- 重试必须带退避，并且不能持有数据库事务或行锁调用外部服务。

## 5. 第三阶段：根据指标做小范围优化（P2）

### 5.1 SSE 优化决策

先用 `qgents.sse.connections`、轮询耗时和 DB 查询次数观察真实连接规模：

- 连接数小于线程池容量：先保留现有模型，仅做超时、异常清理和指标。
- 连接数经常超过线程池容量：将“每连接一个轮询线程”改为共享调度器/共享轮询，再通过用户级或项目级广播分发。
- 改造时保持 `Last-Event-ID`、事件顺序、游标过期语义和现有 SSE 事件契约不变。

### 5.2 EventService 事务边界

以指标确认 `publish()` 是否被成员查询或广播拖慢后，再做以下低风险拆分：

- 事务内只完成序号分配和事件落库。
- 广播继续使用提交后回调或已有 RealtimeHub，不在事务内等待网络发送。
- 不改变事件持久化顺序和 SSE 断线续传语义。

### 5.3 数据库查询

- 先对 SSE 增量查询、任务列表、TaskRun 日志和通知查询执行 `EXPLAIN`。
- 只有确认全表扫描或排序瓶颈后才增加索引。
- 新增索引必须同步初始化脚本和版本化迁移，避免只改本地数据库。

## 6. 测试与验收基线

### 功能回归

- Task 创建、Planner、Coding、Testing、Review、Diff、Push、MR 流程不变。
- TestRun/Dry Run 的失败码和重试语义不变。
- SSE 支持首次连接、断线重连、`Last-Event-ID`、游标过期和客户端关闭。

### 性能验收

至少采集以下基线并对比优化前后：

- HTTP 接口 P50/P95/P99 响应时间；
- 编排总耗时及各阶段 P95；
- Worker、LLM、GitHub 调用 P95；
- 三个执行器的最大 active、最大 queue、rejected 数；
- SSE 活跃连接数、轮询次数、发送失败数；
- TaskRun、TestRun、Push 的成功率和重试率。

第一阶段不要求立刻降低所有耗时；必须先做到失败可定位、队列可见、参数可调。后续调整以 P95 和拒绝率为依据。

## 7. 推荐实施顺序与工作量

| 顺序 | 内容 | 风险 | 预期收益 | 预计耗时 |
|---|---|---|---|---|
| 1 | Actuator/Micrometer 接入 | 低 | 获得全局运行指标 | 0.5 天 |
| 2 | 执行器配置化与线程池指标 | 低 | 立即发现排队和拒绝 | 0.5 天 |
| 3 | Worker/LLM/GitHub/编排统一耗时日志 | 低 | 快速定位慢和失败 | 0.5～1 天 |
| 4 | SSE 指标、异常清理、超时保护 | 低 | 降低连接泄漏和排查成本 | 0.5 天 |
| 5 | 外部调用超时/重试观测 | 中 | 减少瞬时网络故障影响 | 0.5～1 天 |
| 6 | 根据数据决定 SSE 或 DB 小重构 | 中 | 针对真实瓶颈优化 | 1～2 天 |

## 8. 本次明确不做

- 不新增前端接口或改变 SSE payload。
- 不修改 Task、TaskStep、TaskRun、Diff、MR 的业务状态机。
- 不引入 RabbitMQ/Kafka 等新基础设施。
- 不直接把所有线程池扩大到更大值。
- 不凭感觉新增数据库索引。
- 不把 taskId 等高基数标识放进指标标签。

## 9. 完成标准

- 应用启动成功，Actuator 健康检查通过。
- 三个执行器的参数可通过配置调整，队列和拒绝数可观测。
- Task、Worker、LLM、GitHub、TestRun、SSE 至少都有耗时、结果和稳定错误码。
- 失败任务可以通过 `taskId/taskRunId/requestId` 串起完整日志。
- `mvn test`、`git diff --check` 通过，且无数据库、公开接口和业务流程的非计划变更。
- 用一轮真实任务验证：能看出排队耗时、外部调用耗时、重试次数和最终失败点。
