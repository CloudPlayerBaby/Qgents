# CLAUDE.md

@AGENTS.md

## Qgents 后端约定

- 统一技术栈为 `Spring Boot 4.1.0`、`Spring AI 2.0.0`、`LangGraph4j 1.8.20`、`MyBatis-Plus 3.5.17`、`Redis`、`MySQL 8.0.16+` 和 `RabbitMQ`。Redis 服务端版本与 RabbitMQ 版本尚未确认；项目当前未引入 RabbitMQ 客户端依赖，使用前必须先确认版本和接入契约。新增功能优先复用该技术栈，未经明确授权不得引入同类框架或基础设施替代品。
- 实体类必须使用完整、准确的 Javadoc 说明业务含义及其持久化对象或传输对象属性。每个业务字段应说明含义、取值约束、单位或时间语义（适用时），以及与数据库字段或外部数据的映射关系。
- 接口必须使用完整、准确的 Javadoc 说明职责、适用边界和关键前置条件。每个公开方法应描述业务行为、参数含义和约束、返回值语义、副作用，以及异常或失败场景。
- 注释应传达业务语义，不能仅重复类名、字段名、方法名或类型信息；并且必须与已确认的业务 / API 契约和实际实现一致。对尚未确认的信息，标明待确认项或请求澄清，不得自行假定。
